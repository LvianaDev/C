#include <iostream>
using namespace std;

int main() {
    double finalGrade;
    cout << "Enter the final grade: ";
    cin >> finalGrade;

    bool goodBehavior;
    cout << "Is he/she well-behaved? "
    cinn >> goodBehavior

    bool goodStudent = finalGrade >= 9 && goodBehavior;

    return 0;
}